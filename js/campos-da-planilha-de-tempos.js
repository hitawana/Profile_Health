/**
 * Contrato pendente do template .xlsx.
 *
 * Cada item desta lista deverá corresponder a uma coluna real do template.
 * A lista permanece vazia para que TK-01 não introduza campos de corrida que
 * ainda não foram definidos pela fonte de verdade do produto.
 */
export const CAMPOS_DA_PLANILHA_DE_TEMPOS = Object.freeze([]);

